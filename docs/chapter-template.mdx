---
title: "<Chapter NN · Title>"
description: "<One sentence: the concept — and what it lets you say to your agent.>"
sidebar:
  order: NN
---

{/*
  CONCEPT-CHAPTER TEMPLATE (Parts II–IV) — copy this file to start a chapter.
  The 8 sections below are fixed: no additions, no omissions, this order.
  MULTI-LANGUAGE: from Part II, every code listing is Python + TypeScript inside
  a <LangTabs> (synced syncKey="lang"); labels are exactly "Python" then
  "TypeScript". Neutral prose/diagrams stay outside any tab. Part I is
  Python-reference only.
  Pattern chapters (Part III) extend §2–3 with: Desired design features →
  Before → After, and add a "Choosing between X and Y" subsection when the
  chapter pairs patterns. They also add a "## Pattern Cheat Sheet" section
  before Key Takeaways (one <CheatSheet> card per pattern — see below); the
  GENERIC canonical classDiagram lives in the cheat sheet, so §2 uses prose or
  a DOMAIN diagram, not the generic model.
  MDX components (<AICollab>, <VocabTable>, <PromptCard>, <TryIt>, <CheatSheet>,
  <LangTabs>) live in src/components/.
*/}

import { TabItem, Aside } from '@astrojs/starlight/components';
import LangTabs from '../../../components/LangTabs.astro';
import CheatSheet from '../../../components/CheatSheet.astro';

## The Itch

{/* A short, concrete pain scenario — from checkout-lite or an everyday dev
    task (files, configs, orders, notifications). Domain-neutral. The reader
    should recognize the pain before hearing the concept's name. */}

## The Concept

{/* Definition in plain words, then a Mermaid diagram (classDiagram for
    structure; max ~7 nodes). Introduce the canonical name explicitly —
    the name IS the payload. */}

## Before / After

{/* Sequential headings (the reader scrolls the tangle → the design), each
    listing one <LangTabs>: Python then TypeScript, ≤40 lines, runnable,
    idiomatic per language (not transliterated). Single-pattern chapters use
    ### Before / ### After (below). PAIRED-PATTERN chapters nest one level:
    ### <Pattern> → #### Before / #### After, per pattern. */}

### Before

<LangTabs>
  <TabItem label="Python">

```python
# the tangle, Python
```

  </TabItem>
  <TabItem label="TypeScript">

```typescript
// the tangle, TypeScript
```

  </TabItem>
</LangTabs>

### After

<LangTabs>
  <TabItem label="Python">

```python
# the design, Python
```

  </TabItem>
  <TabItem label="TypeScript">

```typescript
// the design, TypeScript
```

  </TabItem>
</LangTabs>

## Language Notes

{/* The one section whose prose is language-specific — wrap the whole body in a
    <LangTabs>. Python tab: Pythonic deflation (functions, Protocols, dataclasses,
    match, generators). TypeScript tab: idiomatic TS (structural typing, function
    literals, Record, readonly, discriminated-union switch). When the lightweight
    form beats the classical class-based one — and when it doesn't. */}

<LangTabs>
  <TabItem label="Python">

{/* Pythonic notes + code */}

  </TabItem>
  <TabItem label="TypeScript">

{/* TypeScript notes + code */}

  </TabItem>
</LangTabs>

## When NOT to Use

<Aside type="caution" title="Right-sizing">
  {/* The over-application failure mode, with a counter-example.
      Every principle/pattern gets its counterweight — no exceptions. */}
</Aside>

## 🤖 AI Collaboration

{/* 6a. Vocabulary table — "You say / The agent hears" */}

| You say | The agent hears |
|---|---|
| "...phrase..." | ...structural intent it activates... |

{/* 6b. Prompt templates — 2–4 copy-paste prompts: context + constraint +
    vocabulary + scope. Include at least one negative constraint
    ("no new dependencies", "don't touch the public API"). Agent-neutral. */}

{/* 6c. Review checklist — what to verify in agent output, as checkboxes. */}

{/* 6d. Agent failure modes — known over/mis-applications of this concept. */}

## Try It With Your Agent

{/* Hands-on exercise: starter file + a prompt to run + grade the result
    against the review checklist above. */}

{/* PATTERN CHAPTERS ONLY (Part III): the end-of-chapter reference card, one
    per pattern. Mermaid / intent / when stay language-neutral; only the code
    splits into a <LangTabs>. Pair the canonical (classical GoF) form with the
    idiomatic form — the book's signature move. Back each card with runnable
    examples/chNN/py/concept_*.py and examples/chNN/ts/concept_*.ts. */}

## Pattern Cheat Sheet

<CheatSheet pattern="<Pattern name>">

{/* ```mermaid classDiagram``` — the GENERIC canonical roles, ≤7 nodes (neutral) */}

{/* **Intent:** one sentence. (neutral) */}

<LangTabs>
  <TabItem label="Python">

{/* **Canonical** — classical GoF skeleton. **Pythonic** — the deflation. */}

  </TabItem>
  <TabItem label="TypeScript">

{/* **Canonical** — classical skeleton. **Idiomatic** — the TS-native form. */}

  </TabItem>
</LangTabs>

{/* **Reach for it when** … · **not when** … (neutral)
    Runnable: examples/chNN/py/concept_*.py · examples/chNN/ts/concept_*.ts */}

</CheatSheet>

## Key Takeaways

{/* 3–5 bullets. End with: glossary terms this chapter added (must match the
    rows added to glossary.yaml in this same change set). */}
